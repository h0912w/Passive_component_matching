#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "Running pre-commit hook: Running tests..."
npm test

# 테스트 실패 시 commit 차단
if [ $? -ne 0 ]; then
  echo ""
  echo "❌ 테스트 실패! Commit이 차단되었습니다."
  echo "테스트를 통과한 후 다시 시도하세요."
  exit 1
fi

echo "✅ 테스트 통과"
